apiVersion: v1
kind: Service
metadata:
  name: property-tax-service
  labels:
    app: property-tax-service
spec:
  selector:
    app: property-tax-service
  ports:
    - port: 8080
      targetPort: 8080
