apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: {{ include "property-tax-service.fullname" . }}
  labels:
    {{- include "property-tax-service.labels" . | nindent 4 }}
spec:
  template:
    metadata:
      labels:
        {{- include "property-tax-service.labels" . | nindent 8 }}
    spec:
      containers:
        - image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          ports:
            - containerPort: {{ .Values.service.port }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          envFrom:
            - configMapRef:
                name: {{ include "property-tax-service.fullname" . }}-config
            - secretRef:
                name: {{ include "property-tax-service.fullname" . }}-secret
                optional: true
